#include "../common/mysql_operations.hpp"

using namespace chat_ns;
int main()
{
    logger::initLogger(false, "", 0);
    UserTable user;
    User u;
    // u.userId = "cxy";
    // u.password = "123456";
    //user.createUser(u);
    // user.getUserById("cxy",u);
    // std::cout<<u.userId;
    
    return 0;
}